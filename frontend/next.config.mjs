/** @type {import('next').NextConfig} */
const nextConfig = {
  // 💡 Placez-le ici, directement au premier niveau
  allowedDevOrigins: ['attain-maggot-phoney.ngrok-free.dev'], 
  
  experimental: {
    // Laissez vide ou gardez vos autres options expérimentales ici
  },
};

export default nextConfig;
